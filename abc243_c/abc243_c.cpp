#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
const double PI = 3.14159265358979;

int main() {

    int N, right_min, left_max;
    right_min = 1000000001;
    left_max = -1;
    string S;
    cin >> N;
    int X[N], Y[N];
    rep(i, N) cin >> X[i] >> Y[i];
    cin >> S;
    map<int, int> right_min;
    map<int, int> left_max;

    rep(i, N) {
        
    }

    return 0;
}