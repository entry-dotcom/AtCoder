#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
const double PI = 3.14159265358979;

int main() {

    int N, M;
    cin >> N >> M;
    vector<vector<int>> (N, vector<int>(N, -1));
    double root_M = sqrt(M); //cout << root_M << "\n";

    
    return 0;
}