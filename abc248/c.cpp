#include<bits/stdc++.h>
using namespace std;

int main() {

    int N, M, K;
    vector<vector<int>> dp(N*M, vector<int>(N));

    for (int i=0; i<N*M; i++) {
        for (int j=0; j<N; j++) {
            dp.at(i).at(j) 
        }
    }

    return 0;
}