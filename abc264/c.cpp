#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
const double PI = 3.14159265358979;

int main() {

    ll H1, W1, H2, W2;
    vector<vector<ll>> data_1(H1, vector<ll>(W1));
    vector<vector<ll>> data_2(H2, vector<ll>(W2));
    cin >> H1 >> W1;

    /*
    rep(i, H1) {
        rep(j, W1) cin >> A.at(i).at(j);
    }
    cin >> H2 >> W2;
    rep(i, H2) {
        rep(j, W2) cin >> B.at(i).at(j);
    }
    */


    return 0;
}